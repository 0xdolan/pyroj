# TaskMaster Sync Notes

Primary planning artifacts:

- `docs/TODO.md` (human-readable source of truth)
- `docs/REFACTOR_TASKMASTER.json` (machine-readable task list)

Keep both files synchronized whenever task statuses or scope change.
