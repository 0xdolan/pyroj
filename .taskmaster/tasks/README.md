# Task Master AI

Import the machine-readable task list from:

- `docs/REFACTOR_TASKMASTER.json`

That file contains `meta`, `tasks`, nested `subtasks`, `dependencies`, and `status` fields suitable for Task Master AI or generic JSON task importers.
